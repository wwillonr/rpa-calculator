# Deploy History

## [v1.0.1] - 2026-01-12
### Fixed
- **Critical:** Solved "400 Bad Request" in production caused by version mismatch between Frontend and Backend.
- **Frontend:** Improved error handling in `ROIWizard.jsx` to display specific backend validation messages.
- **Frontend:** Added "User" column to Project History table.
- **Backend:** Enriched `listProjects` with user emails.

### Changed
- Refactored `authContext` to handle admin roles more gracefully.

## [v1.0.2] - 2026-02-20
### Changed
- **Frontend:** Ocultado campo visual de Isenção OPEX no formulário Step 1 Project Info por ser lido somente estaticamente com o valor padrão `isento` em envio de métricas.
- **Frontend:** Removido o helpText explicativo "Se 'Não'..." do campo "Uso de Licença RPA" na etapa de Complexidade (Step 3). O campo já estava configurado para preenchimento estático ("yes").
- **Frontend:** Refatorado o campo de `Uso de Licença RPA` (Step 3 Complexidade). Transicionado o tipo de Input de `Select` com itens suspensos (`MenuItem`) para um formato imutável de caixa de texto plana tipo `TextField` lendo "Sim (Licença Comercial)".
